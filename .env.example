# Bandsintown partner token used as ?app_id= when fetching upcoming events at
# build time. Without it, scripts/bundle_epk.py keeps the fallback show list
# embedded in epk.source/app.js.
BANDSINTOWN_API_TOKEN=
